import { HighlightMyTextDirective } from './highlight-my-text.directive';

describe('HighlightMyTextDirective', () => {
  it('should create an instance', () => {
    const directive = new HighlightMyTextDirective();
    expect(directive).toBeTruthy();
  });
});
